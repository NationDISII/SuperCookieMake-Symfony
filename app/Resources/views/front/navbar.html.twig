
<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}">MyCookie</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li class="{% if app.request.attributes.get('_active') == 'homepage' %}active{% endif %}"><a href="{{ path('homepage') }}">Home</a></li>
                <li class="{% if app.request.attributes.get('_active') == 'list_cookie' %}active{% endif %}"><a href="{{ path('cookies_list') }}">The cookies</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Hey, {% if is_granted('ROLE_USER') %}{{ app.user.username }}{% else %}Guest{% endif %}<span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        {% if is_granted('ROLE_USER') %}
                        <li class="{% if app.request.attributes.get('_active') == 'account_edit' %}active{% endif %}"><a href="{{ path('user_account') }}">Edit account</a></li>
                        <li><a href="{{ logout_path() }}">Logout</a></li>
                        {% else %}
                        <li class="{% if app.request.attributes.get('_active') == 'login' %}active{% endif %}"><a href="{{ path('user_login') }}">Login</a></li>
                        <li class="{% if app.request.attributes.get('_active') == 'register' %}active{% endif %}"><a href="{{ path('user_register') }}">Register</a></li>
                        {% endif %}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>